import React from 'react';
import { AppTitle, LogoIcon } from './MainTitle.styled';

function MainTitle() {
  return (
    <AppTitle>
      <LogoIcon />
      Phonebook
    </AppTitle>
  );
}

export default MainTitle;
